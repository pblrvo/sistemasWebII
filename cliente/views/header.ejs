<!DOCTYPE html>
<html>
  <head>
    <title>Gametrade</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" />
    <style>
      .search-form {
        position: absolute;
        top: 50%;
        right: 20px;
        transform: translateY(-50%);
      }
      .search-form input[type="search"] {
        width: 200px;
      }
    </style>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <a class="navbar-brand" href="<%= process.env.BASE_URI %>/juegos">Juegos</a>
      <button
        class="navbar-toggler"
        type="button"
        data-toggle="collapse"
        data-target="#navbarNav"
        aria-controls="navbarNav"
        aria-expanded="false"
        aria-label="Toggle navigation"
      >
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item">
            <a class="nav-link" href="<%= process.env.BASE_URI %>/noticias">Noticias</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="<%= process.env.BASE_URI %>/new_game">Nuevo Juego</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="<%= process.env.BASE_URI %>/juegos-gratuitos">Juegos Gratuitos</a>
          </li>
        </ul>
        <form class="form-inline search-form" method="GET" action="<%= process.env.BASE_URI %>/juegos/">
          <input class="form-control mr-sm-2" type="search" placeholder="Buscar por título" aria-label="Buscar" name="search" />
          <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Buscar</button>
        </form>
      </div>
    </nav>
  </body>
</html>
